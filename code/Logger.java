public class Logger {


        //create an object of Logger
        static int LogID;
        private static Logger log = new Logger();

        //make the constructor private so that this class cannot be
        //instantiated
   private Logger(){}

        //Get the only object available
        public static Logger getLogObject(){
        return log;
    }

        public void updateLog(){
string log;
   if(log==null){

   }
   else{//update it in databse and map it with todays date

   }
   LogID++:
    }
}
